import type { Config } from '@react-router/dev/config';

export default {
  appDirectory: 'src', // Your root app directory
  ssr: false, // Set to false for a standard Single Page App
} satisfies Config;
