{
  "name": "@text-game/shared",
  "version": "1.0.0",
  "type": "module",
  "description": "Shared game logic and schema for the text-game project",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    "import": "./dist/index.js",
    "default": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "lint": "eslint . --fix",
    "lint:check": "eslint ."
  },
  "dependencies": {
    "@colyseus/schema": "^3.0.62",
    "xstate": "^5.22.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "rimraf": "^6.0.1"
  }
}
